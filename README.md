# 📚 书境 BookTravel

> 连接每一本好书，开启智慧旅程。一个基于 React + Supabase 的企业级图书漂流与社交平台。

![App Screenshot](https://picsum.photos/seed/booktravel_banner/1200/400)

## ✨ 项目简介

**书境 BookTravel** 致力于解决企业内部或社区间的图书流动问题。通过数字化的方式记录每一本书的漂流轨迹，让阅读不再是一座孤岛。用户可以上架自己的书籍、申请接待他人的书籍、在书友圈分享感悟，并获得成就勋章。

本项目已实现**前后端完全联通**，从单纯的 UI 演示转变为真正可用的全栈 MVP 产品。

## 🚀 核心功能

*   **🔐 身份认证系统**
    *   完整的注册/登录流程（基于 Supabase Auth）。
    *   自动生成用户头像与默认资料。
    *   个人中心资料展示与编辑。

*   **📖 书籍全生命周期管理**
    *   **上架共享**：支持扫码（模拟）或手动录入书籍信息，一键共享到平台。
    *   **书籍发现**：列表模式与地图模式（模拟室内定位）切换。
    *   **申请接待**：查看书籍实时状态（在架/流通中），发起接待申请，自动流转书籍持有权。
    *   **漂流护照**：记录每一本书的经手人、漂流路径和时间。

*   **💬 书友社区**
    *   **动态发布**：分享阅读心情。
    *   **实时评论**：支持针对动态发表评论，实时保存至数据库。
    *   **点赞与互动**：(UI已实现) 支持点赞和赠送虚拟咖啡。

*   **🏆 个人驿站**
    *   **勋章系统**：根据阅读和分享行为解锁成就勋章。
    *   **数据统计**：展示累计传递书籍数量与漂流里程。

## 🛠️ 技术栈

**前端 (Frontend)**
*   **React 19** - 构建用户界面的核心库。
*   **TypeScript** - 提供类型安全保证。
*   **Vite** - 极速的开发服务器与构建工具。
*   **Tailwind CSS** - 实用优先的 CSS 框架，快速构建美观 UI。

**后端 (Backend-as-a-Service)**
*   **Supabase** - 开源 Firebase 替代品。
*   **PostgreSQL** - 强大的关系型数据库。
*   **Supabase Auth** - 用户认证与授权。
*   **RLS (Row Level Security)** - 数据库级别的安全策略。

## 🏁 快速开始

### 1. 环境准备
确保你的本地环境已安装 [Node.js](https://nodejs.org/) (推荐 v18+)。

### 2. 安装依赖
```bash
npm install
```

### 3. 配置 Supabase 后端
1.  登录 [Supabase](https://supabase.com/) 创建一个新项目。
2.  进入项目的 **SQL Editor**。
3.  打开本项目中的 `supabase/schema.sql` 文件，复制全部内容。
4.  在 Supabase SQL Editor 中粘贴并运行，以创建表结构、RLS 策略和预置数据。

### 4. 配置环境变量
复制环境变量示例文件：
```bash
cp .env.example .env
```
打开 `.env` 文件，填入你的 Supabase 项目凭证（可在 Supabase 后台 `Settings` -> `API` 中找到）：
```env
VITE_SUPABASE_URL=你的项目URL
VITE_SUPABASE_ANON_KEY=你的anon_key
```

### 5. 启动开发服务器
```bash
npm run dev
```
访问终端显示的本地地址（通常是 `http://localhost:5173`）即可体验。

## 📂 目录结构

```
/
├── src/
│   ├── components/   # 通用组件
│   ├── contexts/     # React Context (如 AuthContext)
│   ├── lib/          # 第三方库配置 (如 supabase client)
│   ├── pages/        # 页面组件 (Discovery, Shelf, Station...)
│   ├── services/     # API 服务层 (auth, books, community)
│   └── types.ts      # TypeScript 类型定义
├── supabase/
│   └── schema.sql    # 数据库初始化脚本
└── ...
```

## 🔒 账号测试
如果你使用了默认的 SQL 脚本初始化数据库，可以直接使用以下测试账号登录：
- **账号**: `alex@booktravel.com`
- **密码**: `password123`

---
Made with ❤️ by DeepV Code AI
